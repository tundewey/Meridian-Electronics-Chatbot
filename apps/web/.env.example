# Preferred: server-side only. Used by src/app/api/chat/route.ts (no CORS from browser to FastAPI).
API_URL=http://127.0.0.1:8000

# Optional fallback for the same proxy (not required if API_URL is set).
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
